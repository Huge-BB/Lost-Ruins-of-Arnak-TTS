name: Web Engine

on:
  push:
    branches:
      - web-engine/**
    paths:
      - 'web/**'
      - 'objects/AllCards.4bfea4/**'
      - 'objects/Level1Sites.a27f36/**'
      - 'objects/Level2Sites.5ab09c/**'
      - '.github/workflows/web-engine.yml'
  pull_request:
    paths:
      - 'web/**'
      - 'objects/AllCards.4bfea4/**'
      - 'objects/Level1Sites.a27f36/**'
      - 'objects/Level2Sites.5ab09c/**'
      - '.github/workflows/web-engine.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Extract TTS game data
        working-directory: web
        run: npm run extract:data
      - name: Run engine tests
        working-directory: web
        run: npm test
