import { prepareAssistantSupply } from './assistants.ts';
import { prepareBaseGameSetup } from './cards.ts';
import { applyCardEffects, getCardEffects } from './effects.ts';
import { setupLeader, runLeaderRoundStart, runLeaderRoundEnd, addFearToHand, leaderCardDestination } from './leaders/index.ts';
import { captainCallSpecialist, explorerMoveArchaeologist, explorerSpendSnack, falconerReturnEagle, mysticPerformRitual, professorBuyArchiveArtifact } from './leaders/actions.ts';
import { advanceResearchByNode } from './research-action.ts';
import { researchStartNode } from './research-topology.ts';
import { nextLegalResearchPosition, RESEARCH_START_POSITION } from './research.ts';
import { shuffleWithSeed } from './rng.ts';
import { addGuardianFear, resolveRewardCode } from './site-rewards.ts';
import { canPayTravel, hasTravelCost } from './travel.ts';
import type { EngineContext, GameAction, GameState, PlayerColor, PlayerId, Resource } from './types.ts';

const MAX_ROUNDS=5; const PLAYER_COLORS:PlayerColor[]=['Yellow','Green','Blue','Red']; const DISCOVERY_COMPASS_COST={1:3,2:6} as const;
const STARTING_RESOURCES=[{coin:2,compass:0},{coin:1,compass:1},{coin:2,compass:1},{coin:1,compass:2}] as const;
const EMPTY_CONTEXT:EngineContext={cards:{}}; const emptyResources=()=>({tablet:0,arrowhead:0,jewel:0,coin:0,compass:0,fear:0});
export function createGame(playerIds:PlayerId[]):GameState{if(playerIds.length<1||playerIds.length>4)throw new Error('Arnak supports 1-4 players');const players=Object.fromEntries(playerIds.map((id,index)=>[id,{id,name:`Player ${index+1}`,color:PLAYER_COLORS[index],rules:{journalMaxLead:0},resources:emptyResources(),workers:2,availableWorkers:2,hasPassed:false,researchMagnifying:RESEARCH_START_POSITION,researchJournal:RESEARCH_START_POSITION,deck:[],hand:[],discard:[],playedCards:[],idols:[],assistants:[],defeatedGuardians:[]} ]));const startNode=researchStartNode('bird');return{version:1,phase:'setup',round:1,firstPlayer:playerIds[0],currentPlayer:playerIds[0],players,playerOrder:[...playerIds],sites:{},discovery:{level1Deck:[],level2Deck:[],guardianDeck:[],idolDeck:[]},assistants:{stacks:[],specialStack:[]},market:{items:[],artifacts:[],itemDeck:[],artifactDeck:[],exiled:[]},research:{board:'bird',magnifying:Object.fromEntries(playerIds.map(id=>[id,RESEARCH_START_POSITION])),journal:Object.fromEntries(playerIds.map(id=>[id,RESEARCH_START_POSITION])),magnifyingNode:Object.fromEntries(playerIds.map(id=>[id,startNode])),journalNode:Object.fromEntries(playerIds.map(id=>[id,startNode])),templeArrivals:[],templeArrivalPoints:{}},pendingRewards:[]};}
function assertPlaying(s:GameState){if(s.phase!=='playing')throw new Error('Game is not in progress');} function assertPlayer(s:GameState,id:PlayerId){const p=s.players[id];if(!p)throw new Error(`Unknown player: ${id}`);return p;} function assertCurrentPlayer(s:GameState,id:PlayerId){if(s.currentPlayer!==id)throw new Error(`It is not ${id}'s turn`);const p=assertPlayer(s,id);if(p.hasPassed)throw new Error(`${id} has already passed`);}
function addResource(s:GameState,id:PlayerId,r:Resource,a:number){const p=assertPlayer(s,id);if(!Number.isInteger(a)||a<0)throw new Error('Amount must be a non-negative integer');p.resources[r]+=a;} function spendResource(s:GameState,id:PlayerId,r:Resource,a:number){const p=assertPlayer(s,id);if(!Number.isInteger(a)||a<0)throw new Error('Amount must be a non-negative integer');if(p.resources[r]<a)throw new Error(`Insufficient ${r}`);p.resources[r]-=a;}
function nextActivePlayer(s:GameState,from:PlayerId){const i=s.playerOrder.indexOf(from);for(let o=1;o<=s.playerOrder.length;o++){const c=s.playerOrder[(i+o)%s.playerOrder.length];if(!s.players[c].hasPassed)return c;}} function rotateFirstPlayer(s:GameState){const i=s.playerOrder.indexOf(s.firstPlayer);return s.playerOrder[(i+1)%s.playerOrder.length];}
function refillMarketSlot(s:GameState,type:'Item'|'Artifact'){const deck=type==='Item'?s.market.itemDeck:s.market.artifactDeck;const card=deck.shift();if(!card)return;if(type==='Item')s.market.items.push(card);else s.market.artifacts.unshift(card);} function refillMarketForRound(s:GameState){const a=s.market.artifactDeck.splice(0,Math.max(0,s.round-s.market.artifacts.length));if(a.length)s.market.artifacts.unshift(...a);s.market.items.push(...s.market.itemDeck.splice(0,Math.max(0,6-s.round-s.market.items.length)));} function advanceMarketToNextRound(s:GameState){const a=s.market.artifacts.pop(),i=s.market.items.shift();if(a)s.market.exiled.push(a);if(i)s.market.exiled.push(i);s.round+=1;refillMarketForRound(s);}
function setupDiscoveryDecks(s:GameState,c:EngineContext,seed:string){const sites=Object.values(c.sites??{}).filter(x=>x.expansion==='Base Game');s.discovery.level1Deck=shuffleWithSeed(sites.filter(x=>x.level===1).map(x=>x.id).sort(),`${seed}:sites:1`);s.discovery.level2Deck=shuffleWithSeed(sites.filter(x=>x.level===2).map(x=>x.id).sort(),`${seed}:sites:2`);s.discovery.guardianDeck=shuffleWithSeed(Object.values(c.guardians??{}).filter(x=>x.expansion==='Base Game').map(x=>x.id).sort(),`${seed}:guardians`);s.discovery.idolDeck=shuffleWithSeed(Object.values(c.idols??{}).filter(x=>x.expansion==='Base Game').map(x=>x.id).sort(),`${seed}:idols`);}
function playCard(s:GameState,a:Extract<GameAction,{type:'PLAY_CARD'}>,c:EngineContext){assertPlaying(s);assertCurrentPlayer(s,a.playerId);const p=assertPlayer(s,a.playerId),i=p.hand.indexOf(a.cardId);if(i<0)throw new Error('Card is not in the player hand');if(!c.cards[a.cardId])throw new Error(`Unknown card: ${a.cardId}`);p.hand.splice(i,1);p.playedCards.push(a.cardId);applyCardEffects(s,a.playerId,getCardEffects(a.cardId,c));}
function payTravelFromHand(s:GameState,id:PlayerId,ids:string[],c:EngineContext,siteId:string){const p=assertPlayer(s,id),cost=s.sites[siteId].travelCost??{};if(!hasTravelCost(cost)){if(ids.length)throw new Error('Site does not require travel payment');return;}if(!ids.length)throw new Error('Travel payment is required');if(new Set(ids).size!==ids.length)throw new Error('Travel payment contains duplicate cards');for(const card of ids)if(!p.hand.includes(card))throw new Error(`Travel card is not in hand: ${card}`);if(!canPayTravel(cost,ids,c))throw new Error('Travel payment does not satisfy site cost');for(const card of ids){p.hand.splice(p.hand.indexOf(card),1);p.playedCards.push(card);}}
function resolveSite(s:GameState,id:PlayerId,siteId:string,c:EngineContext){const site=s.sites[siteId];if(!site.tileId)return;const d=c.sites?.[site.tileId];if(!d)throw new Error(`Unknown site tile: ${site.tileId}`);if(d.level!==site.level)throw new Error(`Site tile level mismatch: ${site.tileId}`);resolveRewardCode(s,id,site.tileId,d.rewardCode,c);} function takeIdol(s:GameState,id:PlayerId,faceUp:boolean,c:EngineContext){const idolId=s.discovery.idolDeck.shift();if(!idolId)throw new Error('Idol deck is empty');const idol=c.idols?.[idolId];if(!idol)throw new Error(`Unknown idol: ${idolId}`);s.players[id].idols.push({id:idolId,faceUp});if(faceUp)resolveRewardCode(s,id,idolId,idol.rewardCode,c);}
function prepareWorkerForSiteAction(s:GameState,a:Extract<GameAction,{type:'PLACE_WORKER'|'DISCOVER_SITE'}>){const p=assertPlayer(s,a.playerId);if(a.explorerMove){const moved=explorerMoveArchaeologist(s,a.playerId,a.explorerMove.fromSiteId,a.siteId,a.explorerMove.snackId);Object.assign(s,moved);return false;}if(p.availableWorkers<1)throw new Error('No available worker');return true;}
function discoverSite(s:GameState,a:Extract<GameAction,{type:'DISCOVER_SITE'}>,c:EngineContext){assertPlaying(s);assertCurrentPlayer(s,a.playerId);let site=s.sites[a.siteId];if(!site)throw new Error(`Unknown site: ${a.siteId}`);if(site.tileId)throw new Error('Site has already been discovered');if(site.occupiedBy)throw new Error('Site is occupied');const consumesWorker=prepareWorkerForSiteAction(s,a);site=s.sites[a.siteId];const deck=site.level===1?s.discovery.level1Deck:s.discovery.level2Deck;if(!deck.length)throw new Error(`Level ${site.level} site deck is empty`);if(!s.discovery.guardianDeck.length)throw new Error('Guardian deck is empty');const idols=site.level===2?2:1;if(s.discovery.idolDeck.length<idols)throw new Error('Not enough idols to discover site');payTravelFromHand(s,a.playerId,a.paymentCardIds??[],c,a.siteId);spendResource(s,a.playerId,'compass',DISCOVERY_COMPASS_COST[site.level]);if(consumesWorker)s.players[a.playerId].availableWorkers-=1;site.occupiedBy=a.playerId;takeIdol(s,a.playerId,true,c);if(site.level===2)takeIdol(s,a.playerId,false,c);site.tileId=deck.shift()!;resolveSite(s,a.playerId,a.siteId,c);site.guardian=s.discovery.guardianDeck.shift()!;}
function buyCard(s:GameState,a:Extract<GameAction,{type:'BUY_CARD'}>,c:EngineContext){assertPlaying(s);assertCurrentPlayer(s,a.playerId);const card=c.cards[a.cardId];if(!card)throw new Error(`Unknown card: ${a.cardId}`);if(card.type!=='Item'&&card.type!=='Artifact')throw new Error('Card cannot be bought from the market');const row=card.type==='Item'?s.market.items:s.market.artifacts,index=row.indexOf(a.cardId);if(index<0)throw new Error('Card is not available in the market');spendResource(s,a.playerId,card.type==='Item'?'coin':'compass',card.cost??0);row.splice(index,1);const p=assertPlayer(s,a.playerId),destination=leaderCardDestination(s,a.playerId,card);if(destination==='hand')p.hand.push(card.id);else if(destination==='deck')p.deck.push(card.id);else p.playedCards.push(card.id);refillMarketSlot(s,card.type);}
function advanceResearch(s:GameState,a:Extract<GameAction,{type:'ADVANCE_RESEARCH'}>,c:EngineContext){assertPlaying(s);assertCurrentPlayer(s,a.playerId);const p=assertPlayer(s,a.playerId),d=c.researchTracks?.[s.research.board];if(!d)throw new Error(`Research track data required for ${s.research.board}`);if(a.toNodeId){advanceResearchByNode(s,d,{playerId:a.playerId,token:a.track,toNodeId:a.toNodeId,paymentCardIds:a.paymentCardIds},c);return;}const amount=a.amount??1;if(!Number.isInteger(amount)||amount<1)throw new Error('Research amount must be positive');let pos=s.research[a.track][a.playerId],m=s.research.magnifying[a.playerId],j=s.research.journal[a.playerId];for(let step=0;step<amount;step++){pos=nextLegalResearchPosition(d,a.track,pos,m,j,p.rules.journalMaxLead);if(a.track==='magnifying')m=pos;else j=pos;}s.research[a.track][a.playerId]=pos;if(a.track==='magnifying')p.researchMagnifying=pos;else p.researchJournal=pos;}
function cleanupPlayerForNextRound(s:GameState,id:PlayerId,c:EngineContext){const p=s.players[id];runLeaderRoundEnd(s,id,c);p.availableWorkers=p.workers;p.hasPassed=false;if(p.playedCards.length){p.deck.push(...shuffleWithSeed(p.playedCards,`${s.setupSeed??'default'}:round:${s.round}:cleanup:${id}`));p.playedCards=[];}while(p.hand.length<5&&p.deck.length)p.hand.push(p.deck.shift()!);if(p.leader?.id==='mystic')addFearToHand(s,id,c);} function resolveGuardianFear(s:GameState,c:EngineContext){for(const site of Object.values(s.sites))if(site.guardian&&site.occupiedBy)addGuardianFear(s,site.occupiedBy,c);} function finishRound(s:GameState,c:EngineContext){resolveGuardianFear(s,c);for(const id of s.playerOrder)cleanupPlayerForNextRound(s,id,c);for(const site of Object.values(s.sites))delete site.occupiedBy;if(s.round>=MAX_ROUNDS){s.phase='finished';return;}advanceMarketToNextRound(s);s.firstPlayer=rotateFirstPlayer(s);s.currentPlayer=s.firstPlayer;for(const id of s.playerOrder)runLeaderRoundStart(s,id,c);}
export function reduce(state:GameState,action:GameAction,context:EngineContext=EMPTY_CONTEXT):GameState{const next=structuredClone(state);switch(action.type){case'START_GAME':{if(next.phase!=='setup')throw new Error('Game has already started');next.playerOrder.forEach((id,i)=>{const r=STARTING_RESOURCES[i];next.players[id].resources.coin=r.coin;next.players[id].resources.compass=r.compass;});const seed=action.seed??'default';next.setupSeed=seed;next.research.board=action.researchBoard??'bird';const start=researchStartNode(next.research.board);next.research.magnifyingNode=Object.fromEntries(next.playerOrder.map(id=>[id,start]));next.research.journalNode=Object.fromEntries(next.playerOrder.map(id=>[id,start]));setupDiscoveryDecks(next,context,seed);if(context.assistants&&Object.keys(context.assistants).length)next.assistants=prepareAssistantSupply(context.assistants,next.research.board,next.playerOrder.length,seed);if(Object.keys(context.cards).length){const setup=prepareBaseGameSetup(context,next.playerOrder.length,seed);next.market=setup.market;next.playerOrder.forEach((id,i)=>{next.players[id].color=setup.playerDecks[i].color;next.players[id].hand=setup.playerDecks[i].hand;next.players[id].deck=setup.playerDecks[i].deck;});}for(const id of next.playerOrder){const leader=action.leaders?.[id];if(leader)setupLeader(next,id,leader,context,seed);}for(const id of next.playerOrder){if(next.players[id].leader?.id==='mystic')addFearToHand(next,id,context);runLeaderRoundStart(next,id,context);}next.phase='playing';return next;}case'GAIN_RESOURCE':addResource(next,action.playerId,action.resource,action.amount);return next;case'SPEND_RESOURCE':spendResource(next,action.playerId,action.resource,action.amount);return next;case'ADVANCE_RESEARCH':advanceResearch(next,action,context);return next;case'PLAY_CARD':playCard(next,action,context);return next;case'PLACE_WORKER':{assertPlaying(next);assertCurrentPlayer(next,action.playerId);let site=next.sites[action.siteId];if(!site)throw new Error(`Unknown site: ${action.siteId}`);if(site.occupiedBy)throw new Error('Site is occupied');const consumesWorker=prepareWorkerForSiteAction(next,action);site=next.sites[action.siteId];payTravelFromHand(next,action.playerId,action.paymentCardIds??[],context,action.siteId);if(consumesWorker)next.players[action.playerId].availableWorkers-=1;site.occupiedBy=action.playerId;resolveSite(next,action.playerId,action.siteId,context);return next;}case'DISCOVER_SITE':discoverSite(next,action,context);return next;case'BUY_CARD':buyCard(next,action,context);return next;case'LEADER_CAPTAIN_SPECIALIST':assertPlaying(next);assertCurrentPlayer(next,action.playerId);return captainCallSpecialist(next,action.playerId,action.stackIndex);case'LEADER_FALCONER_RETURN_EAGLE':assertPlaying(next);assertCurrentPlayer(next,action.playerId);return falconerReturnEagle(next,action.playerId,action.rewardPosition);case'LEADER_PROFESSOR_BUY_ARCHIVE':assertPlaying(next);assertCurrentPlayer(next,action.playerId);return professorBuyArchiveArtifact(next,action.playerId,action.cardId,context,action.suitcaseCompass??0);case'LEADER_EXPLORER_SPEND_SNACK':assertPlaying(next);assertCurrentPlayer(next,action.playerId);return explorerSpendSnack(next,action.playerId,action.snackId,action.siteId);case'LEADER_MYSTIC_RITUAL':assertPlaying(next);assertCurrentPlayer(next,action.playerId);return mysticPerformRitual(next,action.playerId,action.fearCount);case'END_TURN':{assertPlaying(next);assertCurrentPlayer(next,action.playerId);const following=nextActivePlayer(next,action.playerId);if(!following)throw new Error('All players have passed; round should already be finished');next.currentPlayer=following;return next;}case'PASS':{assertPlaying(next);assertCurrentPlayer(next,action.playerId);next.players[action.playerId].hasPassed=true;const following=nextActivePlayer(next,action.playerId);if(following)next.currentPlayer=following;else finishRound(next,context);return next;}}}
